<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="/public/style.css">
    <style>/* Apply styles to the body */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        
        /* Style the chat container */
        .chat {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            text-align: center;
            max-width: 500px;
        }
        
        /* Style the heading */
        h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 10px;
        }
        
        /* Style the chat information paragraph */
        p {
            font-size: 16px;
            color: #555;
            margin-bottom: 20px;
        }
        
        /* Style the textarea for message editing */
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: vertical;
        }
        
        /* Style the edit button */
        button {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        
        button:hover {
            background-color: #0056b3;
        }
        </style>
</head>
<body>    <div class="chat">
    <h2>Edit: <%= chat._id%></h2>
    <p>This Chat was send from <B><%=chat.from%> </B>to<b> <%=chat.to%></b></p>
    <form method="POST" action="/chats/<%=chat._id%>?_method=PUT" >
        <textarea cols="20" rows="10" name="msg"><%=chat.msg%></textarea><br>

        <button>Edit</button>
    </form>

</div>
</body>
</html>